# June Priority Action Plan Design

The June report will replace the placeholder at the end of section 06 with the
priority action plan shown in the supplied reference. The implementation will
compose the existing `SubSection` and `ListItemC` components so the new content
inherits the report's established typography, rules, colors, spacing, and
responsive behavior.

The subsection will preserve the reference order and copy:

1. R—02 — This month — Search Console, referral audit, Business Profile
2. R—01 — This week — data cleanup & event verification
3. R—03 — Ongoing — confirmed-lead tracking & local landing pages

No shared component APIs, styles, assets, or routes will change. Verification
will format and lint the page, run the production build, and compare the
rendered section with the supplied screenshot.
